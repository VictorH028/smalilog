[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "smalilog"
version = "0.1.0"
description = "Herramienta CLI para inyección Smali y registro remoto"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
smalilog = "smalilog.main:cli"

